version: "3"
services:
  app:
    container_name: translate-server
    restart: always
    build: ./
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    links:
      - mongo
  mongo:
    container_name: mymongo
    image: mongo
    ports:
      - "27017:27017"